## 1. 基础设置

- [x] 1.1 在 `format_builder.py` 中添加响应式阈值常量 `RESPONSIVE_THRESHOLD = 1200`
- [x] 1.2 在 `FormatBuilder.__init__` 方法中添加窗口宽度缓存变量

## 2. 布局切换核心实现

- [x] 2.1 创建 `_update_button_layout()` 方法，实现根据窗口宽度切换按钮布局的逻辑
- [x] 2.2 创建 `_on_window_configure()` 事件处理方法，监听窗口大小变化
- [x] 2.3 在 `_create_widgets()` 方法中绑定窗口的 `<Configure>` 事件
- [x] 2.4 修改按钮的初始打包逻辑，使用 `_update_button_layout()` 方法

## 3. 节流与性能优化

- [x] 3.1 在 `_on_window_configure()` 中实现节流逻辑，使用 `after()` 延迟执行
- [x] 3.2 添加宽度变化阈值检查，只在宽度变化超过10像素时触发布局切换
- [x] 3.3 缓存上一次的窗口宽度，避免重复计算

## 4. 测试与验证

- [ ] 4.1 测试宽屏模式（≥1200像素）下按钮水平排列
- [ ] 4.2 测试窄屏模式（<1200像素）下按钮垂直排列
- [ ] 4.3 测试窗口大小变化时布局自动切换
- [ ] 4.4 测试按钮功能在两种布局模式下均正常工作
- [ ] 4.5 测试频繁窗口大小变化时的性能表现

**注意**: 以上测试任务需要用户手动验证。