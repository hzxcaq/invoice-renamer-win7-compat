## 5. 信息汇总表全量列合并

- [x] 5.1 修改 `ExcelReader.read_excel_merged()` 合并逻辑
 - 去掉 `商品名称` 新列，改用原始列名 `货物或应税劳务名称`
 - 值仍提取商品名（去 `*分类*` 前缀）
- [x] 5.2 合并信息汇总表所有 8 个独有列
 - 货物或应税劳务名称、规格型号、单位、数量、单价、税率、税收分类编码、特定业务类型
 - 每列取金额最大的明细行的值
- [x] 5.3 更新 FormatBuilder 分组信息
 - 信息汇总表组显示 8 个独有列
- [x] 5.4 更新单元测试验证新列名和全量列

- [x] 1.1 在 `ExcelReader` 中新增 `read_excel_merged(file_path)` 方法
  - 读取所有 sheet 的表头和数据
  - 按 `数电发票号码` 列建立映射
- [x] 1.2 实现信息汇总表聚合逻辑
  - 按 `数电发票号码` 分组
  - 每组取 `金额` 最大的行
  - 提取 `货物或应税劳务名称`，去掉 `*分类*` 前缀
  - 新列命名为 `商品名称`
- [x] 1.3 实现建筑服务合并逻辑
  - 按 `数电发票号码` LEFT JOIN
  - 取独有列：`建筑服务发生地`、`建筑项目名称`
- [x] 1.4 合并结果：返回扁平 `List[Dict]` + 分组信息 `Dict[str, List[str]]`
  - 分组信息格式: `{"发票基础信息": ["序号", ...], "信息汇总表": ["商品名称"], "建筑服务": ["建筑服务发生地", ...]}`

## 2. 格式构建器树状改造

- [x] 2.1 在 `FormatBuilder` 中将左侧 `Listbox` 替换为 `ttk.Treeview`
  - 树状结构：一级节点为 sheet 名，二级节点为列名
  - 使用 tag 实现选中/未选中状态切换（颜色区分）
- [x] 2.2 实现点击行切换选中状态
  - 点击列名行：切换选中/取消
  - 点击分组行：展开/折叠
  - 选中后自动加入已选列列表（避免重复）
- [x] 2.3 新增 `update_columns_grouped(columns, grouped_columns)` 方法
  - `columns`: 合并后的全部列名列表
  - `grouped_columns`: 按 sheet 分组的列名字典
  - 保持原有 `update_columns()` 方法兼容单 sheet 场景
- [x] 2.4 默认展开第一个分组，折叠其他分组

## 3. 主窗口集成

- [x] 3.1 修改 `MainWindow.set_excel_file()` 方法
  - 尝试调用 `read_excel_merged()`（多 sheet）
  - 如果失败或只有 1 个 sheet，退化为 `read_excel()`（单 sheet）
- [x] 3.2 将分组信息传递给 `FormatBuilder.update_columns_grouped()`
- [x] 3.3 更新 Excel 预览区域显示合并后的数据

## 4. 测试

- [x] 4.1 单元测试：`ExcelReader.read_excel_merged()` 的合并逻辑
  - 测试金额最大行选择
  - 测试商品名提取（去分类前缀）
  - 测试空值处理
  - 测试单 sheet 兼容
- [x] 4.2 集成测试：使用 `4月/2026.04取得发票.xlsx` 真实数据验证
  - 验证合并结果有 132 行
  - 验证 `商品名称` 列有值
  - 验证建筑服务列正确合并
- [x] 4.3 GUI 测试：验证树状选择器交互正常
